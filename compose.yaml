services:
  mysql:
    image: mysql:8.4
    container_name: transcendence_mysql
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "${MYSQL_PORT}:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: